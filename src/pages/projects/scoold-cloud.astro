---
import BaseLayout from "../../layouts/BaseLayout.astro";
import ProjectPage from "../../components/ProjectPage.astro";
import marketing from "../../data/site-data.json";

const project = marketing.site.pages.projectsIndex.items.find((item) => item.id == "scoold-cloud");
const baseUrl = marketing.site.organization.url;

const productJsonLd = {
  "@context": "https://schema.org",
  "@type": "Product",
  name: project.name,
  description: project.page.shortDescription,
  brand: {
    "@type": "Organization",
    name: marketing.site.organization.name
  },
  url: "https://erudika.com/projects/scoold-cloud"
};

const breadcrumbJsonLd = {
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  itemListElement: [
    { "@type": "ListItem", position: 1, name: marketing.site.components.breadcrumbs.home, item: baseUrl },
    { "@type": "ListItem", position: 2, name: marketing.site.components.breadcrumbs.projects, item: `${baseUrl}/projects` },
    { "@type": "ListItem", position: 3, name: project.name, item: `${baseUrl}/projects/scoold-cloud` }
  ]
};
---

<BaseLayout
  title={marketing.site.meta.projects.scooldCloud.title}
  description={marketing.site.meta.projects.scooldCloud.description}
  canonical={marketing.site.meta.projects.scooldCloud.canonical}
  jsonLd={[productJsonLd, breadcrumbJsonLd]}
>
  <ProjectPage project={project} />
</BaseLayout>