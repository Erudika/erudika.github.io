---
import BaseLayout from "../layouts/BaseLayout.astro";
import marketing from "../data/site-data.json";

const { privacy } = marketing.site.pages;
---

<BaseLayout
  title={marketing.site.meta.privacy.title}
  description={marketing.site.meta.privacy.description}
  canonical={marketing.site.meta.privacy.canonical}
>
  <section class="py-16">
    <div class="section-shell">
      <p class="text-xs font-semibold uppercase tracking-[0.3em] text-slate-500">{privacy.eyebrow}</p>
      <h1 class="mt-3 text-4xl font-semibold text-base-content">{privacy.title}</h1>
      <p class="mt-4 text-base-content/70">{privacy.body}</p>
      <div class="mt-6 space-y-4 text-sm text-base-content/70">
        {privacy.points.map((point) => (
          <p>{point}</p>
        ))}
      </div>
    </div>
  </section>
</BaseLayout>
